# Crimson Nail
這是個遊戲專案，用C++20編譯的，主要目的是示範`ToyBox`這個專案如何製作遊戲程式。
核心內容是卡牌遊戲，然後添加一些些額外元素。

## 使用許可
程式碼是以`MIT`協議發佈的，但是圖片檔的版權依然屬於原作者。

## 專案建置方式
本專案並沒有依賴CMake，
你可以把CMake相關文件當成專案架構的描述檔，
然後用你自己的做法來建立專案也行。

### 最簡單的選擇
在Windows下，最簡單的做法是下載MinGW跟CMake，然後可以就很簡單的編譯本專案了，
因為MinGW版本的library已經被編譯好並放在專案裡了，
MinGW版本建議可以選TDM-GCC 10.3.0，因為安裝過程超簡單，體積也不大(約600MB)，
CMake只要3.15版以上就沒問題，都是免安裝程式，
作業系統版本至少需要Windows 7，更舊的就不行了。
把這段拿去給AI看，AI每一步都能教你。

### 正規作法
本專案也能用`Visual Studio 2022`編譯，但是你需要參閱`ToyBox`這個專案了，
必須自己去準備需要的函式庫，這會麻煩很多。
