# Crimson Nail
這個遊戲專案主要目的是示範、驗證`ToyBox`函式庫可以用來製作遊戲程式，
因此專案結構非常的簡約、好理解，不像正式C++專案那樣完備、齊全，
連同說明文件也是盡可能的簡約，
核心玩法是卡牌遊戲，然後添加一些額外元素。

## 專案建置方式
一定要安裝有支援 C++20 的編譯器，這點無論如何都無法省略。

### 最簡單的選擇
1. 安裝MinGW，推薦選用TDM-GCC 10.3.0，因為安裝過程極其簡單，安裝體積也不過約600MB而已
2. 安裝 CMake 3.15 以上的版本
3. 用 CMake 建立 makefile 來編譯專案(第三方函式庫已經事先用MinGW編譯好並放在專案裡了)

### 正規作法
本專案也能用`Visual Studio 2022`編譯，
但是你必須自己去準備需要的函式庫，
CMake設定檔也需要修改，事情會變得非常麻煩。

## 運行平台
目前只支援`Windows 7`以上的微軟作業系統，未來預計會跨到`Linux`上。

## 使用許可
程式碼是以`MIT`協議發佈的，但是圖片檔、音檔的版權依然屬於原作者。
